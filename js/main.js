(function ($) {
    "use strict";

    
        // Initialize the carousel
        var carousel = new bootstrap.Carousel(document.getElementById('testimonial-carousel'), {
            interval: 5000, // Adjust interval as needed (milliseconds)
            wrap: true // Enable looping of testimonials
        });

    
    
})(jQuery);

